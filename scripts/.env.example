# ADAS Diagnostics Co-pilot Configuration

# Database Configuration
DATABASE_URL=postgresql://adas_user:adas_password@localhost:5435/adas_diagnostics
# Example for Podman containers: postgresql://adas_user:adas_password@localhost:5435/adas_diagnostics

# Neo4j Configuration for Knowledge Graph
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=adas_neo4j_password

# LLM Provider Configuration (Using Gemini AI as default)
# Set this to either openai, openrouter, ollama, or gemini
LLM_PROVIDER=gemini

# Google API Key for Gemini AI (get from https://aistudio.google.com/app/apikey)
GOOGLE_API_KEY=your-google-api-key-here
LLM_API_KEY=your-google-api-key-here

# The LLM you want to use for the agents. Make sure this LLM supports tools!
# Gemini example: gemini-2.0-flash, gemini-1.5-pro, gemini-1.5-flash
# OpenAI example: gpt-4o-mini
# OpenRouter example: anthropic/claude-3-5-sonnet
# Ollama example: qwen2.5:14b-instruct
LLM_CHOICE=gemini-2.0-flash

# Embedding Provider Configuration (keeping OpenAI for embeddings)
# Set this to either openai or ollama
EMBEDDING_PROVIDER=openai

# Base URL for embedding models
# OpenAI: https://api.openai.com/v1
# Ollama: http://localhost:11434/v1
EMBEDDING_BASE_URL=https://api.openai.com/v1

# API Key for embedding provider (OpenAI for embeddings)
EMBEDDING_API_KEY=sk-your-openai-api-key-here
OPENAI_API_KEY=sk-your-openai-api-key-here

# The embedding model you want to use for RAG
# OpenAI example: text-embedding-3-small
# Ollama example: nomic-embed-text
EMBEDDING_MODEL=text-embedding-3-small

# Ingestion-specific LLM (can be different/faster model for processing)
# Leave empty to use the same as LLM_CHOICE
INGESTION_LLM_CHOICE=gemini-1.5-flash

# Application Configuration
APP_ENV=development
LOG_LEVEL=INFO
APP_PORT=8058

# Automotive-Specific Configuration
# Maximum number of documents to process in a single batch
MAX_BATCH_SIZE=50

# Vector search configuration
VECTOR_SEARCH_LIMIT=10
SIMILARITY_THRESHOLD=0.7

# Knowledge graph configuration
GRAPH_SEARCH_LIMIT=20
MAX_RELATIONSHIP_DEPTH=3

# Automotive data processing
SUPPORTED_FILE_TYPES=.md,.txt,.pdf,.csv,.json
MAX_FILE_SIZE_MB=100

# OTA Update processing
OTA_VERSION_PATTERN=v\d+\.\d+\.\d+
DTC_CODE_PATTERN=[BPUC]\d{4}

# Session management
SESSION_TIMEOUT_MINUTES=60
MAX_CHAT_HISTORY=100
